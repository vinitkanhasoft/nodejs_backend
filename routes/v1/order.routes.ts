import { Router } from 'express';

const router = Router();

// Example placeholder for order routes
router.get('/', (req, res) => {
  res.json({ message: 'Get all orders - to implement' });
});

export default router;