import { Router } from 'express';

const router = Router();

// Example placeholder for user routes
router.get('/', (req, res) => {
  res.json({ message: 'Get all users - to implement' });
});

export default router;